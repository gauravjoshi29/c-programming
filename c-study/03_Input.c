#include <stdio.h>
// Program to add two number in C
int main()
{
    int a, b;

    printf("Enter number a: ");
    scanf("%d", &a);

    printf("Enter number b: ");
    scanf("%d", &b);

    printf("The sum is: %d \n", a + b);
    return 0;
}
